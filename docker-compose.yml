services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: shiroscan-api:dev
    ports:
      - "8080:8080"
    environment:
      APP_ENV: development
      LOG_LEVEL: INFO
      MAX_UPLOAD_MB: "10"
      OCR_PROVIDER: ${OCR_PROVIDER:-gemini}
      LLM_PROVIDER: ${LLM_PROVIDER:-gemini}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      CORS_ALLOWED_ORIGINS: "*"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/api/health').read()"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: artifacts/shiroscan/Dockerfile
    image: shiroscan-web:dev
    ports:
      - "5173:5173"
    environment:
      # In Docker, the Vite dev proxy must target the 'api' service name
      VITE_DEV_API_PROXY: http://api:8080
      VITE_API_URL: http://localhost:8080
    depends_on:
      api:
        condition: service_healthy
